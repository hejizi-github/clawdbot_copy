# Session 26 Plan

## Goal
Implement near-duplicate loop detection — detect "almost-loops" where step sequences repeat with minor variations (e.g., `A B C` → `A B D` → `A B C`), which exact n-gram matching misses.

## Context
- Session 25 review (9/10 PASS) confirmed deterministic metrics have converged to docstring-level polish
- Reviewer explicitly suggested near-duplicate loop detection as the next capability
- Current loop detection only finds exact n-gram matches — real agent loops often have slight variations per iteration

## Approach
1. **Minor polish from Session 25 review** (prerequisite cleanup):
   - Fix `_is_subpattern` docstring: "repeated" → "contiguous subsequence of" 
   - Add why-comment for `positions[1:]` (first occurrence isn't wasted)

2. **Near-duplicate loop detection**:
   - Add `loop_similarity_threshold: float = 1.0` to `MetricConfig` (1.0 = exact only = backward compatible)
   - Implement `_hamming_similarity(a, b)` — fraction of matching positions between two n-grams of same length
   - Implement `_find_near_duplicate_clusters(ngrams, threshold)` — group n-grams by similarity
   - If a cluster's total occurrences >= min_repeats, report as `near_loops_found` in details
   - Near-loop positions contribute to `total_repeated_steps` penalty (same as exact loops)
   - Ensure exact loops (threshold=1.0) produce identical results to current behavior

3. **Files modified**:
   - `trajeval/trajeval/metrics.py` — new functions + integrate into `loop_detection()`
   - `trajeval/tests/test_metrics.py` — new tests for near-duplicate scenarios

## Verification
- `python -m pytest tests/ -q` — all existing 316 tests still pass (backward compat)
- New tests cover: near-duplicate detection, threshold tuning, exact-match-only mode, edge cases
- Target: +6-8 new tests
