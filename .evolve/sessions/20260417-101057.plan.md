# Session Plan: Fix Weak Assertions + Automated Prevention

## 目标
修复测试套件中所有弱断言（`assert A or B` 模式），并创建自动化检查工具阻止未来复发。这是连续 5+ session 的 #1 评审问题。

## 方案

### 1. 修复弱断言（8 处）

| File | Line | Current | Fix |
|------|------|---------|-----|
| test_cli.py:1024 | `assert score != score2 or len(m) == len(m2)` | Tautology — or 右侧恒真 | 直接 assert trace_id 不同 |
| test_cli.py:157 | `assert "total_steps=" in out or "total_tool_calls=" in out` | 可能合理（两种格式）| 需要读上下文判断 |
| test_cli.py:487 | `assert "skipping" in out or "Warning" in out` | 可能合理 | 需要读上下文判断 |
| test_cli.py:703 | `assert "total_ste" in out or "failed=0" in out` | 可能合理 | 需要读上下文判断 |
| test_e2e.py:113 | `assert "##" in out or "Metric" in out` | 可能合理 | 需要读上下文判断 |
| test_improvement.py:309 | `assert "loop" in or "repetitive" in` | 语义正确（检查关键词） | 保留或用 any() |
| test_improvement.py:485 | `assert "safe" in or "unsafe" in` | 语义正确 | 保留或用 any() |
| test_ci_output.py:183 | `assert "::notice" in or "::warning" in` | 语义正确 | 保留或用 any() |

### 2. 创建自动化工具
- `tools/check_weak_assertions.sh` — 扫描 `assert.*or True`, `assert.*or False`, 和可疑的 `assert A or B` 模式
- 添加到 config 的 verification.commands

### 3. 提交
- 所有测试仍须 440 passed
- git commit 到远程仓库

## 验证
- `python -m pytest trajeval/tests/ -q` — 440 passed
- `bash tools/check_weak_assertions.sh` — 0 tautologies detected
