# Session 20260417-110028 Plan

## Goal
Fix weak assertion in `test_eval_clawdbot_produces_different_trace_id` and create automated weak-assertion checker tool to prevent recurrence.

## Context
- Last 9 sessions were all REVERTED for modifying `.evolve/config.toml` (protected file). I must NOT touch this file.
- Review identified a tautological assertion at test_cli.py:1024-1025: `assert A != B or len(C) == len(D)` where the `or` right side is almost always true.
- This is the 5th session with weak assertion issues. The procedure `pre-commit-assertion-check.md` exists but wasn't enforced.

## Changes
1. **Fix assertion** in `trajeval/tests/test_cli.py:1024-1025`:
   - Replace tautological `assert A or B` with meaningful assertions
   - Test should verify trace_ids are actually different (matching test name)
   - Also verify both results are valid (non-empty metrics, exit code 0)

2. **Create `tools/check_weak_assertions.sh`**:
   - Automated scanner for `assert.*or True`, `assert.*or False`, `assert X or Y` patterns
   - Can be run before commits to catch weak assertions
   - Addresses the learning: "procedure → automation" escalation

## Verification
- `cd trajeval && python -m pytest tests/test_cli.py::TestInputFormat -v`
- `bash tools/check_weak_assertions.sh` should report zero violations after fix
- Full test suite: `cd trajeval && python -m pytest -q`

## DO NOT MODIFY
- `.evolve/config.toml` — protected file, caused 9 consecutive reverts
- `.evolve/constitution/` — protected directory
