# Session 20260417-063051 Plan — Phase 3 Session 15

## Goal
Add `latency_budget` as the 6th deterministic metric + fix compare CLI's weak recovery_window test.

## Why this is the highest-impact change
- Reviewer explicitly recommended latency_budget as next priority (completes all 6 deterministic metrics from proposal)
- Compare CLI recovery_window test gap has been flagged — it's the 3rd time eval/compare test asymmetry appeared
- Both items directly address the reviewer's top suggestions

## Changes

### 1. Fix compare CLI recovery_window test (test_cli.py)
- Replace `test_recovery_window_flag_accepted` with a proper test that verifies recovery_window value flows into the error_recovery metric details
- Use recovery_trace.json with narrow (1) vs wide (5) window values, verify details.recovery_window matches

### 2. Add latency_budget metric (metrics.py)
- New function: `latency_budget(trace, budget_ms) -> MetricResult`
- Score formula: `min(budget_ms / actual_duration_ms, 1.0)` — under budget = 1.0, over budget = proportional penalty
- When no budget provided (None): return score 1.0 with mode="no_budget"
- When duration is 0: return score 1.0 with mode="no_duration"

### 3. MetricConfig update (metrics.py)
- Add field: `latency_budget_ms: float | None = None`

### 4. Engine integration (metrics.py evaluate())
- Add `latency_budget(trace, budget_ms=config.latency_budget_ms)` to results list

### 5. CLI flags (cli.py)
- Add `--latency-budget` option (float, in ms) to both `eval` and `compare` commands
- Wire into MetricConfig

### 6. Tests
- Unit: empty trace, under budget, over budget, no budget, zero duration
- Unit: evaluate() includes latency_budget, config flow-through
- CLI eval: verify --latency-budget value appears in output details
- CLI compare: verify --latency-budget value flows through to metric output

## Verification
- `cd trajeval && python -m pytest -q` — all tests pass, count increases from 196
- New metric appears in evaluate() output for all existing fixtures
- CLI flags produce different outputs with different budget values
