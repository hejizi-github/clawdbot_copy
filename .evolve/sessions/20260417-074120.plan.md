# Session 20260417-074120 Plan

## Goal
Fix 2 review issues + add `--format ci` output for GitHub Actions integration (CI-ready annotations and markdown summary).

## Rationale
- The 2 review fixes are trivial but maintain quality standards (type consistency, UX warning)
- CI integration is the most differentiating feature from the proposal — it enables `trajeval` to run as a CI step and produce inline PR annotations
- This directly advances the "code output ↑" KPI while opening a new usage dimension

## Changes

### Part 1: Review fixes (surgical)
1. **`trajeval/scorer.py:252`** — Change `_aggregate_dimensions(results, aggregation: str)` → `aggregation: Literal["median", "mean"]`
2. **`trajeval/cli.py:129-133`** — When `judges == 1` and `aggregation != "median"`, emit `click.echo` warning that aggregation is ignored in single-judge mode

### Part 2: CI output format (new feature)
3. **`trajeval/trajeval/ci_output.py`** (new) — CI formatting functions:
   - `format_eval_ci(report, threshold)` → GitHub Actions `::error::`/`::warning::`/`::notice::` commands + markdown summary
   - `format_compare_ci(result)` → annotations for regressions/improvements + markdown summary
4. **`trajeval/trajeval/cli.py`** — Add `"ci"` to `--format` choices for `eval` and `compare` commands
5. **`trajeval/tests/test_ci_output.py`** (new) — Tests for CI output formatting

### CI Output Format Spec
```
::error title=trajeval: loop_detection FAIL::score=0.45, threshold=0.70 (3 loops detected)
::notice title=trajeval: step_efficiency PASS::score=0.95
---
## trajeval Evaluation Summary
| Metric | Score | Status |
|--------|-------|--------|
| step_efficiency | 0.95 | ✅ PASS |
| loop_detection | 0.45 | ❌ FAIL |
...
**Overall: 0.72 — FAIL**
```

## Verification
```bash
cd trajeval && python -m pytest -q
```
All existing 273 tests must pass + new CI output tests.
