# Session Plan: 20260417-040642

## Goal
Start Phase 3 by renaming the project from "AgentLens" to "trajeval" (available on PyPI) and building the complete project skeleton with trace data models, JSON ingester, CLI skeleton, and test suite — corresponding to MVP Session 1 in the proposal.

## Actions
1. **Rename**: Update `strategies/project-proposal.md` to reflect new name "trajeval"
2. **Project setup**: Create `trajeval/` directory with:
   - `pyproject.toml` (build config, dependencies, CLI entry point)
   - `trajeval/__init__.py` (package init)
   - `trajeval/models.py` (Pydantic data models: AgentTrace, TraceStep, TokenUsage)
   - `trajeval/ingester.py` (Simple JSON trace ingester)
   - `trajeval/cli.py` (CLI skeleton with `trajeval eval <trace.json>`)
3. **Test suite**: Create `tests/` with:
   - `tests/conftest.py` (shared fixtures, sample traces)
   - `tests/test_models.py` (model validation tests)
   - `tests/test_ingester.py` (ingester parsing tests)
   - Target: 5+ unit tests
4. **Sample data**: Create `tests/fixtures/` with sample trace JSON files

## Verification
- `cd trajeval && python -m pytest tests/ -v` — all tests pass
- `python -m trajeval.cli --help` — CLI shows help
- `ruff check trajeval/` — no lint errors
