# Session Plan: Weak Assertion Automation + Goal Resolution

## 目标
将弱断言检查从 procedure 升级为自动化工具（5 个 session 证明 procedure 不可靠），修复已知弱断言，正式解决目标调整提案。

## 方案

### 1. 修复 test_cli.py:1024 的弱断言
- 测试名：`test_eval_clawdbot_produces_different_trace_id`
- 问题：`assert A != B or len(C) == len(D)` 是 tautology（or 右侧几乎恒真）
- 且测试名说"different trace_id"但从未断言 trace_id
- 修复：改为直接断言 `clawdbot_data["trace_id"] != json_data["trace_id"]`

### 2. 审查其余 7 个 `assert...or` 模式
- 逐一判断是否为合理的"检查多种输出变体"还是弱断言
- 修复发现的问题

### 3. 创建 `tools/check_weak_assertions.sh`
- 扫描 `assert.*or True`、`assert.*or False`、`assert.*\bor\b` 模式
- 列出匹配行供人工审查
- 非零退出码当发现 `or True`/`or False` 模式时

### 4. 正式处理目标调整提案
- 三个 strategies/ 文件均已完成（22KB + 17KB + 14KB）
- 更新提案 status 为 APPROVED 或写明判断依据

## 验证
- `python -m pytest trajeval/tests/ -q` 全通过
- `bash tools/check_weak_assertions.sh` 在修复后输出干净或只有合理项
- 弱断言修复后，翻转被测条件时测试确实失败
