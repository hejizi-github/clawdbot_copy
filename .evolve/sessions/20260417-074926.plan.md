# Session 20260417-074926 Plan

## Goal
Add `--format ci` to the `judge` command (review Priority 1) and fix code style issues from last review (Priority 2).

## Changes

### 1. `ci_output.py` — new `format_judge_ci` function
- Accept `JudgeResult | EnsembleResult`, `threshold: float`, `passed: bool`
- For each dimension: annotation level based on score (0-2=error, 3=warning, 4-5=notice)
- Markdown summary table with dimension scores and explanations
- Ensemble variant: include judge count, aggregation method, std dev column
- Add `ComparisonResult` type annotation to `format_compare_ci`

### 2. `cli.py` — wire `--format ci` into judge command
- Add `"ci"` to judge's `click.Choice`
- Call `format_judge_ci` in the ci branch
- Preserve existing exit code logic

### 3. `test_ci_output.py` — fix imports + new tests
- Replace `__import__("unittest.mock", ...)` with `from unittest.mock import patch`
- Add `TestFormatJudgeCI` class: single judge annotations, ensemble annotations, threshold/pass logic
- Add CLI integration test for `judge --format ci`

## Verification
- `python -m pytest -q` — all tests pass, count should increase by ~10+
