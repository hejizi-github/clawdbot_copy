# Session 20260417-103137 Plan

## 目标
修复 `test_eval_clawdbot_produces_different_trace_id` 的 tautological 断言 + 创建弱断言自动检查工具

## 背景
- 评审连续 5 个 session 发现弱断言问题，procedure 已证明无效，需要升级为自动化工具
- L1024: `assert A != B or len(C) == len(D)` — or 右侧恒真，测试名不副实
- 最近 5 个 session 因修改 config.toml 被回滚 — 本次绝对不碰 protected files

## 方案

### 1. 修复断言 (test_cli.py:1024-1025)
替换为直接验证 trace_id 不同：
```python
assert clawdbot_data["trace_id"] == "clawdbot-test-001"
assert json_data["trace_id"] == "test-trace-001"
assert clawdbot_data["trace_id"] != json_data["trace_id"]
```
同时验证两个输入产生了不同的 overall_score（证明评估确实针对不同内容执行了）。

### 2. 创建 tools/check_weak_assertions.sh
自动扫描 `assert ... or True`、`assert ... or False`、`assert A or B` 模式中的可疑 tautology。
输出匹配行供人工审查，非零退出码表示有可疑项。

### 3. 添加 verification.commands 到 config — 不做！
config.toml 是 protected file，不能修改。工具在 tools/ 下可用即可。

## 验证
- `python -m pytest trajeval/tests/test_cli.py -k "test_eval_clawdbot_produces_different_trace_id" -v` — 通过
- `python -m pytest trajeval/tests/ -v` — 全部通过
- `bash tools/check_weak_assertions.sh` — 无 tautology 输出
