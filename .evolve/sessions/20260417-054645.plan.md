# Session 20260417-054645 Plan — Phase 3 Session 10

## Goal
Add boundary/edge-case tests and fix all code quality issues from the Session 9 review.

## Why this is the highest-impact work
The review explicitly lists boundary tests as the #1 improvement area. Empty traces, large traces, and malformed inputs are where real-world failures happen — these are the scenarios that unit tests on happy paths miss. The three code fixes (unused `tmp_path`, CLI mock inconsistency, missing LICENSE) are low-effort/high-signal cleanups.

## Plan

### 1. Boundary tests in `tests/test_integration.py` (~10 new tests)
- **Empty trace** (0 steps): verify eval, judge, and compare all handle gracefully
- **Single-step trace**: minimal valid input
- **Large trace** (50+ steps): verify performance stays under 1s (proposal: "<5s for 50-step traces")
- **Missing fields trace**: no `trace_id`, no `agent_name`, no `task`
- **Malformed JSON input**: verify IngestError raised with clear message
- **All-error-steps trace**: all steps are type="error"

### 2. CLI judge test fix
- Replace `@patch("trajeval.cli.judge")` with `monkeypatch` injecting FakeAnthropicClient at `trajeval.scorer.anthropic` level, so CLI → judge() → fake client runs the full chain

### 3. Code cleanup
- Remove unused `tmp_path` from `test_full_calibration_with_correlation` and `test_calibration_with_disagreement`

### 4. Add LICENSE file
- MIT license as declared in README

## Verification
- `cd trajeval && python -m pytest tests/ -q` — all tests pass
- Test count increases by ~10 (159 → ~170)
- Performance: large trace test completes in < 1s
