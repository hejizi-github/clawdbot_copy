# Session 20260417-052244 Plan

## 目标
修复 reviewer 上次指出的 3 个问题：Rich markup 泄漏到 click.prompt、私有函数跨模块导入、缺失的 annotate/calibrate CLI 集成测试。

## 方案

### 1. Fix Rich markup bug (cli.py:222-224)
- `click.prompt(f"Score for [cyan]{dim}[/cyan] (0-5)")` → 用 `click.style(dim, fg="cyan")` 替代 Rich markup
- click.style() 是 click 原生的 ANSI 着色，在 click.prompt 中有效

### 2. Rename _load_judge_results → load_judge_results (calibration.py:70, cli.py:14)
- 去掉下划线前缀，因为它是 CLI 需要的公共接口
- 更新 cli.py 的 import

### 3. Add annotate CLI integration test (test_cli.py)
- 用 CliRunner + input 参数模拟用户交互输入分数
- 验证 annotations JSONL 文件被正确创建
- 验证输出包含 "Saved" 确认信息

### 4. Add calibrate CLI integration test (test_cli.py)
- 创建临时 annotations.jsonl 和 judgments.jsonl fixtures
- 验证 table 输出包含 Spearman 相关信息
- 验证 JSON 格式输出可解析且包含预期字段

### 5. [ ] 测试骨架已创建（经验检查项）

## 验证
- `python -m pytest --tb=short -q` 全部通过
- 新增测试数量 >= 4（annotate 至少 2 + calibrate 至少 2）
- 总测试数 129 → 133+
- 手动确认 click.prompt 不再包含 Rich markup 语法
