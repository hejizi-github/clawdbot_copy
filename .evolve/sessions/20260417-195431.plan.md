# Session 20260417-195431 Plan

## Goal
Add OpenTelemetry (OTLP) JSON trace format support to trajeval — the last remaining MVP feature from the project proposal (Section 5, Session 5 scope).

## Why This Is the Right Next Step
- Last 5 sessions were ALL reverted for touching config.toml — this session focuses purely on code
- The goal adjustment proposal is PENDING; the three ACTIVE goals are effectively complete (strategies/ deliverables exist)
- OTLP ingestion is the last unimplemented MVP feature from `strategies/project-proposal.md`
- It adds real value: enables trajeval to consume traces from any OpenTelemetry-instrumented agent framework

## Changes
1. **`trajeval/ingester.py`** — Add `ingest_otlp()` function that parses OTLP JSON export format:
   - Parse `resourceSpans[].scopeSpans[].spans[]` structure
   - Map GenAI semantic convention attributes (`gen_ai.*`) to TraceStep fields
   - Build span tree (parent-child) to reconstruct agent execution sequence
   - Extract token usage from `gen_ai.usage.*` attributes

2. **`trajeval/cli.py`** — Add `otlp` to the `--input-format` choices for all commands

3. **`tests/test_ingester.py`** — Add 10+ tests for OTLP ingestion:
   - Valid OTLP trace with GenAI spans
   - Tool call spans as children of LLM spans
   - Missing/incomplete attributes
   - Error spans
   - Empty/malformed input

## Verification
- `python -m pytest tests/test_ingester.py -v` — all new + existing tests pass
- `python -m pytest` — full suite still passes (440+)
- Manual: `trajeval eval sample_otlp.json --input-format otlp` works
