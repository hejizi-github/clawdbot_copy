# Session 20260417-062206 Plan

## Goal
Wire `recovery_window` into MetricConfig → evaluate() → CLI, fixing the config consistency gap flagged by last review (9/10 → target 9.5+).

## Changes

### 1. MetricConfig (metrics.py:26-31)
- Add `recovery_window: int = 3` field

### 2. evaluate() (metrics.py:270)
- Change `error_recovery(trace)` → `error_recovery(trace, recovery_window=config.recovery_window)`

### 3. CLI eval command (cli.py:33-37)
- Add `--recovery-window` option (int, default 3)
- Pass to MetricConfig constructor

### 4. CLI compare command (cli.py:134-140)
- Add `--recovery-window` option (same as eval)
- Pass to MetricConfig constructor

### 5. error_recovery docstring (metrics.py:176-179)
- Add note clarifying consecutive error semantics: each error independently checks its window

### 6. Test fix (test_metrics.py:390)
- Rename `first_error_recovered` → `recovered_count`

### 7. New tests
- Test that MetricConfig.recovery_window flows through evaluate() and changes results
- Test CLI --recovery-window flag changes output

## Verification
- `cd trajeval && python -m pytest tests/ -q` — all pass, count >= 194
- Verify config with window=1 vs window=5 produces different scores on recovery_trace fixture
