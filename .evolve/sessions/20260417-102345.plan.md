# Session 20260417-102345 Plan

## Goal
Fix weak assertion tautology in `test_eval_clawdbot_produces_different_trace_id` and create automated weak-assertion detection tool to prevent the 6th recurrence.

## Context
- Review identified `assert A != B or len(C) == len(D)` as tautology (or-right almost always true)
- 8 `assert...or...` patterns exist across tests — need to audit each
- Experience notes: weak assertions recurring for 5+ sessions, procedure `pre-commit-assertion-check.md` exists but hasn't been executed — need to escalate to automation
- **CRITICAL**: DO NOT modify `.evolve/config.toml` — last 4 sessions reverted for this

## Changes

### 1. Fix `test_eval_clawdbot_produces_different_trace_id` (test_cli.py ~L1024)
- Replace the tautological `assert A != B or len(C) == len(D)` with meaningful assertions:
  - Assert both commands succeed
  - Assert trace_id values differ (the test's stated purpose)
  - Assert both produce valid metrics

### 2. Audit all 8 `assert...or...` patterns
For each, ask: if left side is False and right side is True, does the test passing still make sense?
- L157: `"total_steps=" in output or "total_tool_calls=" in output` — OK, either field proves trace was parsed
- L487: `"skipping" in output.lower() or "Warning" in output` — OK, either signals the expected warning
- L703: `"total_ste" in output or "failed=0" in output` — SUSPICIOUS, truncated string
- L1024: TAUTOLOGY, fix required
- test_e2e.py:113: `"##" in output or "Metric" in output` — OK, either proves markdown report was generated
- test_ci_output.py:183: `"::notice" in output or "::warning" in output` — OK, either is valid CI annotation
- test_improvement.py:309/485: checking for keyword synonyms in recommendation text — OK, NLP-style

### 3. Create `tools/check_weak_assertions.sh`
- Script that greps for dangerous assertion patterns
- Can be run manually or added as pre-commit hook
- Patterns: `assert.*or True`, `assert.*or False`, suspicious `assert X or Y` where Y is always true

## Verification
- `cd trajeval && python -m pytest tests/ -q` — all tests pass
- `bash tools/check_weak_assertions.sh` — returns clean (no false positives on legitimate patterns)
- Manual review: each fixed assertion would fail if the tested behavior changed
