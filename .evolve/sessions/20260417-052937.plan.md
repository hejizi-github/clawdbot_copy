# Plan: Comprehensive README Documentation

## Goal
Write production-quality README for trajeval so the project is externally usable by developers who've never seen the codebase.

## Why this is the highest-priority item
- Evaluator explicitly recommended: "项目已具备完整的 eval/judge/compare/annotate/calibrate CLI 闭环，建议优先补充 README 文档"
- The current README is 19 lines — install + 2 CLI examples only
- Without docs, the trace JSON format is hidden in `ingester.py` comments; no one can use the tool
- All 5 CLI commands, CI exit code semantics, and Python API are undocumented

## What to document
1. **Overview**: one-paragraph pitch + feature list
2. **Installation**: base, `[judge]`, `[stats]`, `[all]`, `[dev]` extras
3. **Quick Start**: create a sample trace → run eval → interpret output
4. **Trace Format**: full JSON schema with field descriptions (from `models.py`)
5. **CLI Reference**: all 5 commands with options and example invocations
   - `eval` — deterministic metrics
   - `judge` — LLM-as-judge
   - `compare` — regression detection
   - `annotate` — human annotation collection
   - `calibrate` — human-vs-judge correlation
6. **CI Integration**: exit code semantics (0=pass, 1=fail/regression), --threshold, --format json
7. **Metrics**: what each of the 4 deterministic metrics measures
8. **Python API**: programmatic usage example

## What NOT to include
- Contributing guide (premature for v0.1)
- Changelog (no releases yet)
- Badges (no PyPI/CI yet)

## Files to modify
- `trajeval/README.md` — full rewrite

## Verification
- `python -m pytest trajeval/tests/ -q` — confirm tests still pass (no code changes)
- Manual review: all CLI commands mentioned in cli.py are documented
- Manual review: trace format matches models.py
- Manual review: all pyproject.toml extras are documented
