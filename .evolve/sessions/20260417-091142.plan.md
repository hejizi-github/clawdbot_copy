# Session Plan: E2E Integration Test + Finding.metric Fix

## 目标
验证 trajeval 作为完整工具可安装可运行：修复 Finding.metric 前缀不一致，然后创建 e2e 测试验证从 `pip install` 到 CLI 输出的完整流程。

## 方案

### Step 1: Fix Finding.metric prefix (5 min)
- `improvement.py`: `Finding(metric=name, ...)` → `Finding(metric=f"judge:{name}", ...)`
- `tests/test_improvement.py` + `tests/test_cli.py`: 更新断言中对应的 metric 值
- 关闭上次评审的遗留 bug

### Step 2: E2E Integration Tests (main focus)
- 创建 `tests/test_e2e.py`
- 测试场景:
  1. `trajeval eval` 命令对真实 trace JSON 文件的完整流程
  2. `trajeval report` 生成 JSON/markdown 输出
  3. `trajeval compare` 两份报告的对比
  4. Exit code 验证（pass/fail 阈值）
  5. `--ci` 模式输出 GitHub Actions annotation 格式
- 使用 Click CliRunner 模拟真实 CLI 调用（不 mock 内部模块）
- 创建 fixture trace 文件供 e2e 使用

### Step 3: Verify
- `cd trajeval && pip install -e . && python -m pytest -q`
- 确认所有测试（含新 e2e）通过

## 验证
- 所有 391+ 测试通过
- 新增 e2e 测试覆盖 eval/compare/report 的完整 CLI 流程
- Finding.metric 与 metric_summary key 可通过前缀直接关联
