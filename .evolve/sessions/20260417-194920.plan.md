# Session 20260417-194920 Plan

## Goal
Recover 4 major features (OTLP ingestion, batch eval, SQLite storage, CLI enhancements) from orphaned commit 81d045e onto master. These features were developed and tested across multiple sessions but never landed because every session also touched `.evolve/config.toml` and got reverted.

## Problem
- 5 consecutive sessions reverted for modifying protected `.evolve/config.toml`
- 2,600+ lines of tested code (16 files) exist on orphaned commit 81d045e
- Master is missing: OTLP trace ingestion, batch evaluation, SQLite storage, expanded CLI

## Approach
1. `git checkout 81d045e -- trajeval/` to bring all trajeval/ code changes onto master
2. Do NOT touch any `.evolve/config.toml` or `.evolve/constitution/` files
3. Run full test suite to verify everything works
4. Commit only the trajeval/ changes

## Verification
- `python -m pytest trajeval/tests/ -q` — all tests must pass
- Verify no `.evolve/config.toml` in staged files
- Test count should increase significantly (from 440 baseline)

## Risk Mitigation
- Only checkout trajeval/ directory, nothing from .evolve/
- Verify git diff --cached does not include config.toml before committing
