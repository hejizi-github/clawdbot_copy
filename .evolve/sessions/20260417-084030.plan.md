# Session 29 Plan: Fix test accuracy + cleanup + trend sorting

## 目标
修复评审指出的 5 个问题：测试命名/注释与行为矛盾、未使用符号残留、trend 检测缺少排序保证。

## 方案

### 1. 修复 `test_medium_fail_rate_generates_medium_priority` (test_improvement.py:51)
- 当前问题：测试名说 "generates medium priority"，但断言的是 NOT generated；数据是 2/3=66.7% 失败率，注释却写 "33%"
- 修复：改为真正测试 medium 阈值的数据（4 fail + 6 pass = 40%），断言确实生成 `frequently_failing` finding 和 MEDIUM recommendation

### 2. 修复 `test_exact_medium_threshold` (test_improvement.py:62)
- 当前问题：只验证 fail_rate 数学，没有断言业务逻辑（是否生成 finding/recommendation）
- 修复：增加断言 `frequently_failing` finding 存在且 recommendation 为 MEDIUM

### 3. 移除 `_SCORE_MEDIUM` 常量 (improvement.py:70)
- 定义了但未使用

### 4. 移除 `MetricResult` 导入 (improvement.py:10)
- 导入了但未引用

### 5. 为 trend 检测添加排序保证 (improvement.py:74)
- 给 EvalReport 添加可选的 `timestamp` 字段
- `analyze_results` 入口处：如果有 timestamp，按时间排序；否则保持输入顺序
- 添加相应测试

## 验证
```bash
cd /Volumes/MOVESPEED/workspace/agent-lab/trajeval && python -m pytest tests/ -q
```
全部通过 + 新测试覆盖修复点
