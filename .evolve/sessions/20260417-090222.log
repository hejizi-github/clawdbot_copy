Session 31 complete. Fixed all 3 bugs from Session 30's review:

1. **Scale-aware color display** — `_print_improvement_report` now normalizes scores by `scale` (1.0 for deterministic metrics, 5.0 for judge) before applying color thresholds. A score of 2.0/5 (40%) now correctly shows as red, not green.
2. **Judge-only CLI analysis** — `eval_files` is now optional; validates that at least one of `eval_files` or `--judge-files` is provided.
3. **Namespace collision prevention** — Judge metric_summary keys prefixed with `judge:` to prevent silent overwrites when merging with deterministic metrics.

+2 tests (389 -> 391), all passing. Set IDLE — these were the last actionable review items; next session should pivot to higher-value work (e2e integration tests, packaging, or the compare command).
