# Session 20260417-104428 Plan

## Goal
Fix all weak assertions in test suite and create automated weak-assertion checker tool to prevent recurrence (5th time this issue has appeared).

## Context
- Review flagged `test_eval_clawdbot_produces_different_trace_id` (line 1024): `assert A != B or len(C) == len(D)` is a tautology
- `assert...or...` pattern found in 8 locations across test files
- Previous procedure (pre-commit-assertion-check.md) exists but is not being followed
- Need to upgrade from procedure → automation (as per experience: learning < procedure < automation)

## Changes

### 1. Fix tautology in test_cli.py:1024-1025
- Replace `assert overall_score != ... or len(metrics) == ...` with direct `assert trace_id` comparison
- The JSON output includes `trace_id` field — test should assert `clawdbot_data["trace_id"] != json_data["trace_id"]`

### 2. Audit and tighten other `assert...or...` patterns
- Line 157: `"total_steps=" or "total_tool_calls="` — verify which is correct, tighten
- Line 487: `"skipping" or "Warning"` — verify actual output, tighten
- Line 703: `"total_ste" or "failed=0"` — truncated prefix suspicious, fix
- Lines 183, 113, 309, 485: review each, fix if weak

### 3. Create `tools/check_weak_assertions.sh`
- Scans for `assert.*or True`, `assert.*or False`, `assert.*\bor\b` patterns
- Can be run manually before commits
- This is the automation upgrade from procedure

### 4. Add e2e test with Clawdbot JSONL through full eval pipeline
- As suggested by review: use fixture to run full eval, verify metric values are reasonable

## Verification
- `cd trajeval && python -m pytest tests/ -q` — all tests pass
- `bash tools/check_weak_assertions.sh` — reports 0 weak assertions (or only validated ones)
- No modifications to `.evolve/config.toml`

## Critical constraint
- DO NOT modify `.evolve/config.toml` (7 consecutive sessions reverted for this)
