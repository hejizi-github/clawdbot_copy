# Plan: Fix weak assertions + create automated checker

## Goal
Fix the tautology assertion in `test_eval_clawdbot_produces_different_trace_id` and create `tools/check_weak_assertions.sh` to prevent recurrence — addressing the review's #1 suggestion and the 5-session recurring learnings gap.

## Problem Analysis
- `test_cli.py:1024`: `assert clawdbot_data["overall_score"] != json_data["overall_score"] or len(clawdbot_data["metrics"]) == len(json_data["metrics"])` — both scores are 1.0 and both have 6 metrics, so `False or True = True`. The test name says "produces different trace_id" but never checks trace_id.
- `test_cli.py:703`: `assert "total_ste" in result.output or "failed=0" in result.output` — "total_ste" is a suspicious truncation of "total_steps".
- Pre-commit procedure exists but has failed to prevent weak assertions for 5 consecutive sessions → upgrade to automation.

## Changes
1. **`trajeval/tests/test_cli.py:1024`** — Replace tautology with: `assert clawdbot_data["trace_id"] != json_data["trace_id"]` (verify different trace IDs as the test name promises)
2. **`trajeval/tests/test_cli.py:703`** — Fix truncated string, use proper assertion
3. **`tools/check_weak_assertions.sh`** — Create script that greps for `assert.*\bor\b`, `or True`, `or False` in test files, with exit code 1 if found, for use as pre-commit gate
4. Add a real e2e test running Clawdbot JSONL through full eval pipeline to verify metric values (review suggestion #2)

## Verification
- `python -m pytest tests/ -q` — all tests pass
- `bash tools/check_weak_assertions.sh` — exits 0 after fixes (no false positives on acceptable disjunctions)
