# Session 20260417-071417 Plan

## Goal
Expand LLM-as-judge scorer from 2→5 dimensions and add bias mitigation via randomized dimension ordering.

## Why This Is Highest Value
- Deterministic metrics module is feature-complete (8 consecutive 9/10 sessions, reviewer says "转向更高价值方向")
- LLM-as-judge module is the biggest gap: proposal specifies 5 dimensions, only 2 implemented
- Adding dimensions is bounded, testable, and directly addresses the proposal's Session 3 scope
- Bias mitigation (randomized ordering) is a key differentiator cited in section 3.3 of the proposal

## Changes
1. **`trajeval/trajeval/scorer.py`**:
   - Add 3 dimension prompts: `tool_use_appropriateness`, `information_synthesis`, `harm_avoidance`
   - Add `randomize_order` option to `JudgeConfig` (default True)
   - Shuffle dimension order in `build_user_prompt` when `randomize_order=True`
   - Update `JudgeConfig.dimensions` default to include all 5 dimensions

2. **`trajeval/tests/test_scorer.py`**:
   - [ ] Test all 5 dimension prompts appear in user prompt
   - [ ] Test randomized ordering produces different orderings (statistical)
   - [ ] Test `randomize_order=False` preserves original order
   - [ ] Test judge works with all 5 dimensions (mock API response)
   - [ ] Test custom dimension subset selection still works

## Verification
- `python -m pytest trajeval/ -q` — all tests pass (234 + new tests)
- Manual inspection: dimension prompts match proposal's descriptions
