Session 4 complete. Here's the summary:

**What changed:**
1. **`trajeval compare <baseline> <current>`** — new command for regression detection with table/json/markdown output and tolerance-based regression flagging (exit 1 if regression). This is the key CI integration feature.
2. **`judge --threshold`** — added CI gate parity: `judge` now exits non-zero when `overall_score < threshold`, matching `eval` command behavior.
3. **Review cleanup** — fixed mutable default in `JudgeConfig.dimensions` (now uses `default_factory`), replaced fragile line-based code fence stripping with regex.

**Metrics:** 89 tests (19 new), ruff clean, all review items from last session addressed.

**Next session:** Session 5 per proposal — calibration module (human annotation CLI + correlation analysis) or alternatively the remaining 3 judge dimensions. The compare command completes the core CI pipeline story: eval + judge + compare.
