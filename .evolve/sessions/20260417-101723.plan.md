# Session Plan: Fix weak assertion + create automated assertion checker

## 目标
修复 `test_eval_clawdbot_produces_different_trace_id` 的弱断言（review 直接要求），并创建 `tools/check_weak_assertions.sh` 自动化脚本（5+ session 反复出现弱断言问题，procedure 未能阻止，需升级为自动化）。

## 为什么这是最值得做的事
1. 弱断言是连续 5 个 session 的头号反复问题，review 再次明确指出
2. learnings 中"learning < procedure < automation"的升级路径要求创建自动化工具
3. 上次 3 个 session 因修改 config.toml 被回滚——本次绝不碰 config.toml

## 方案

### 1. 修复弱断言 (test_cli.py L1024-1025)
**当前**: `assert clawdbot_data["overall_score"] != json_data["overall_score"] or len(clawdbot_data["metrics"]) == len(json_data["metrics"])`
- 测试名说"produces_different_trace_id"，但断言检查 overall_score
- `or` 右侧 `len(metrics) == len(metrics)` 恒真，导致整个断言永远通过

**修复为**: `assert clawdbot_data["trace_id"] != json_data["trace_id"]`
- 验证数据: clawdbot trace_id="clawdbot-test-001", json trace_id="test-trace-001" — 确实不同

### 2. 创建 tools/check_weak_assertions.sh
扫描 tests/ 目录中的弱断言模式:
- `assert.*\bor\b` (析取式断言)
- `or True` / `or False` (布尔短路)
- 输出匹配行供人工审查，非零退出码表示发现问题

### 3. 执行 pre-commit-assertion-check procedure
提交前用 grep 扫描所有 `assert.*or` 模式，验证无新增弱断言

## 验证
- `cd trajeval && python -m pytest tests/test_cli.py::TestInputFormat::test_eval_clawdbot_produces_different_trace_id -v` 通过
- `cd trajeval && python -m pytest -q` 全部 440 测试通过
- `bash tools/check_weak_assertions.sh` 运行成功，不再报告修复后的测试

## 不做的事
- 不修改 .evolve/config.toml（上 3 次 session 的回滚原因）
- 不新增功能（本次聚焦修复 + 工具化）
