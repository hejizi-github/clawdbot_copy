# Session 20260417-050311 Plan — CLI Integration Tests + Threshold Consistency

## 目标
补全 CLI 集成测试（CliRunner 测 eval/judge/compare 的 exit code），统一 threshold 默认值，补 compare 指标不对齐的边界测试。

## 方案

### 1. 统一 threshold 默认值
- **文件**: `trajeval/trajeval/cli.py` L84
- **改动**: `judge --threshold` 默认值从 0.6 改为 0.7，与 eval/compare 一致
- `_print_judge_report` 的默认参数也从 0.6 → 0.7

### 2. CLI 集成测试（新文件）
- **文件**: `trajeval/tests/test_cli.py`（新建）
- **测试用例**:
  - `test_eval_pass_exit_0` — 高质量 trace，threshold=0.3 → exit 0
  - `test_eval_fail_exit_1` — threshold=0.99 → exit 1
  - `test_eval_invalid_file_exit_1` — 无效 JSON → exit 1
  - `test_eval_json_format` — --format json 输出可解析
  - `test_judge_exit_code` — mock scorer，score >= threshold → exit 0
  - `test_judge_fail_exit_code` — mock scorer，score < threshold → exit 1
  - `test_compare_no_regression_exit_0` — 相同 trace 比较 → exit 0
  - `test_compare_regression_exit_1` — baseline 好于 current → exit 1
  - `test_compare_markdown_format` — --format markdown 输出包含 markdown 表格

### 3. 指标不对齐边界测试
- **文件**: `trajeval/tests/test_compare.py`（追加）
- **测试用例**: baseline 有 metric_a，current 有 metric_b → 缺失方用 0.0 填充

## 验证
```bash
cd trajeval && python -m pytest tests/ -v && python -m ruff check .
```
