# Session Plan: --compare-baseline for eval command

## Goal
Connect eval with the storage module for automatic regression detection in CI.

## Approach
- Add `--compare-baseline` flag to `eval` command
- When set: lookup last stored result for the same agent, compare, report regressions
- Implies `--save` so the new result is always persisted
- Exit code 1 if regression detected
- JSON output includes `baseline_comparison` with metric deltas

## Files Modified
- `trajeval/trajeval/cli.py` — add --compare-baseline and --tolerance options
- `trajeval/tests/test_cli.py` — 7 new tests

## Verification
- 556 tests pass (549 + 7 new)
- Weak assertion checker clean
