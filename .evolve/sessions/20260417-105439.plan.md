# Session 20260417-105439 Plan

## 目标
修复 `test_eval_clawdbot_produces_different_trace_id` 的弱断言 + 创建自动化弱断言检查工具

## 背景
- 上次评审 (8.6/10) 指出 `assert A != B or len(C) == len(D)` 是 tautology
- 弱断言问题已跨 5+ session 反复出现，procedure 未能阻止
- 经验教训明确指出需要从 procedure 升级为 automation

## 方案

### 1. 修复弱断言 (test_cli.py L1024-1025)
- 当前: `assert clawdbot_data["overall_score"] != json_data["overall_score"] or len(clawdbot_data["metrics"]) == len(json_data["metrics"])`
- 问题: or 右侧几乎恒真，测试永远通过；且测试名说的是 trace_id 但断言没检查 trace_id
- 修复: 改为直接断言 `trace_id` 不同（与测试名一致），并独立断言两个结果都有有效结构

### 2. 创建 tools/check_weak_assertions.sh
- 自动扫描 `assert.*\bor\b` 和 `or True` 模式
- 对发现的匹配报告行号和内容
- 将来可作为 pre-commit hook 使用

### 3. 全量扫描已有弱断言
- 审查 grep 发现的其他 7 个 `assert.*or` 匹配
- 区分合理的 OR（检查多种输出格式）和 tautology

## 验证
- `cd trajeval && python -m pytest tests/ -v` 全部通过
- `bash tools/check_weak_assertions.sh` 不报告 tautology

## 约束
- **不修改 .evolve/config.toml**（前 8 个 session 因此被 revert）
