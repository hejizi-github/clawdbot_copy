# Session 20260417-073405 Plan

## Goal
Fix 4 accuracy issues from last review + expose `--aggregation` CLI flag. All are recurring patterns (input validation, Literal types, boundary behavior).

## Issues to Fix
1. **Remove unused `import math`** (scorer.py:6)
2. **`EnsembleConfig.aggregation: str` → `Literal["median", "mean"]`** (scorer.py:236) — reject invalid values at validation time
3. **Even judges explanation/score mismatch** (scorer.py:270-272) — find judge whose score is closest to aggregated score, not fixed `sorted_pairs[len//2]`
4. **`--judges` accepts 0/negative** (cli.py:108) — add `click.IntRange(min=1)`
5. **Expose `--aggregation mean|median` CLI flag** (cli.py) — pass through to EnsembleConfig

## Files to Modify
- `trajeval/trajeval/scorer.py` — issues 1-3
- `trajeval/trajeval/cli.py` — issues 4-5
- `trajeval/tests/test_scorer.py` — tests for even-judges explanation, invalid aggregation
- `trajeval/tests/test_cli.py` — test for judges IntRange, aggregation flag

## Verification
```bash
cd trajeval && python -m pytest tests/ -v
```
