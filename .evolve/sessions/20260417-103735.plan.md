# Plan: Fix weak assertions + create automated checker

## Goal
Fix the review-flagged tautological assertion and create `tools/check_weak_assertions.sh` to prevent weak assertion recurrence (5+ sessions with this issue, procedure alone failed to prevent it).

## Changes

### 1. Fix `test_eval_clawdbot_produces_different_trace_id` (test_cli.py:1024)
- **Current**: `assert score_a != score_b or len(metrics_a) == len(metrics_b)` — tautology since both are equal
- **Fix**: `assert clawdbot_data["trace_id"] != json_data["trace_id"]` — matches the test name's intent

### 2. Tighten other `assert...or` patterns
- Line 157: `"total_steps=" or "total_tool_calls="` — both present, assert both independently
- Line 703: `"total_ste" or "failed=0"` — truncated string + or; assert both independently
- Lines 309, 485 (test_improvement.py): semantically valid disjunctions (keyword synonyms) — leave as-is
- Line 487: `"skipping" or "Warning"` — error output, acceptable disjunction — leave as-is
- Line 113 (test_e2e.py): `"##" or "Metric"` — markdown output, acceptable — leave as-is
- Line 183 (test_ci_output.py): `"::notice" or "::warning"` — CI format varies — leave as-is

### 3. Create `tools/check_weak_assertions.sh`
- Scan tests/ for `assert.*or True`, `assert.*or False`, `assert.*\bor\b` patterns
- Flag matches for manual review
- Exit non-zero if dangerous patterns found

## Verification
- `python -m pytest trajeval/tests/ -q` — all 440 tests pass
- `bash tools/check_weak_assertions.sh` — no dangerous patterns flagged
