# Session 20260417-065801 Plan (Phase 3 Session 18)

## 目标
为 `trajeval eval` 的 table 输出添加 details 展示能力，让用户无需切换到 JSON 即可看到每个指标的诊断详情。同时修复上轮评审指出的 markdown spacing 微瑕。

## 方案

### 1. Markdown spacing fix (compare.py:139)
在 `_format_details_section` 中 Baseline 和 Current 之间加空行分隔。

### 2. Eval table details (cli.py `_print_report`)
- 给 `eval` 命令添加 `--details` flag（默认关闭，避免信息过载）
- 当启用时，在 scores table 新增 "Details" 列
- 格式化策略：将 details dict 渲染为紧凑的 `key=value` 对（跳过冗余的 mode 字段），用逗号分隔
- 新增 `_format_details_compact(details: dict) -> str` 辅助函数

### 修改文件
- `trajeval/trajeval/compare.py` — 加空行 (1 行改动)
- `trajeval/trajeval/cli.py` — 添加 `--details` flag + `_format_details_compact()` + 修改 `_print_report()`
- `trajeval/tests/test_compare.py` — 更新 details section 测试验证空行
- `trajeval/tests/test_cli.py` — 添加 `--details` flag 的测试（有 details / 无 --details 不显示）

## 验证
- `python -m pytest` 全部通过
- 手动测试 `trajeval eval sample.json --details` 确认 table 渲染
- 测试计数从 218 增长（预计 +4~6）

## Checklist（对照已有模式）
- [ ] CLI 参数与已有 flag 风格一致（--format, --threshold 等）
- [ ] _format_details_compact 是纯函数，有独立测试
- [ ] table 输出宽度在终端 80 列内可读
- [ ] 无 details 时 flag 不影响已有输出
