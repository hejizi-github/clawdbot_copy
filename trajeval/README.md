# trajeval

Framework-agnostic agent trajectory evaluation.

Ingest execution traces from any agent framework, score with deterministic metrics + LLM-as-judge, output CI-ready quality reports.

## Install

```bash
pip install trajeval
```

## Quick Start

```bash
trajeval eval trace.json
trajeval eval trace.json --format json
```
